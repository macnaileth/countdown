'use client';

import { TimeMachine, Timer } from "../components/TimeMachine";

export default function Home() {
  return (
    <main className="flex flex-col items-center justify-center p-24">
        <TimeMachine />
    </main>
  );
}
